pipeline {
  agent any
  tools {
    maven 'maven'
  }
  stages {
    
    stage ('build&test') {
      steps {
        sh ''' 
        java --version
        mvn clean package
        '''
        
      }
    }
  }
}
